import Logo from "./logo";



export default function Header () {

    return (
        <header id="header">
            <Logo />
            <h2 className="page-title">Return on Ad Spend (ROAS) Calculator</h2>
        </header>
    )
}